---
sidebar_position: 2
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Examples

## Minimal usage

### Play ground

```jsx live
function App() {
  const items = [...Array(100)].map((_, i) => i);
  const cardRect = { width: 300, height: 200 };
  return (
    <div style={{ height: 300 }}>
      <CardWindow data={items} cardRect={cardRect}>
        {Card}
      </CardWindow>
    </div>
  );
}
```

### Sample Card

The CardProps definition is [here](./api/modules#cardprops).

<Tabs>
<TabItem value="js" label="JavaScript">

```jsx
const Card = ({ data, index, style }) => (
  <div className="card" style={{ ...style }}>
    <div className="card__body">
      <h2>{data[index]}</h2>
    </div>
  </div>
);
```

</TabItem>
<TabItem value="py" label="TypeScript">

```tsx
const Card: React.FC<CardProps> = ({ data, index, style }) => (
  <div className="card" style={{ ...style }}>
    <div className="card__body">
      <h2>{data[index]}</h2>
    </div>
  </div>
);
```

</TabItem>
</Tabs>

### Required properties

CardWindow has 3 required properties. Type parameter is `T extends any[]`.

| Name       | Type                                          |
| :--------- | :-------------------------------------------- |
| `cardRect` | [`Rect`](./api/modules#rect) | - |
| `children` | `React.ComponentType`<[`CardProps`](./api/modules#cardprops)<`T`\>\> | - |
| `data` | `T` | `data` is an array. CardWindow passes data to `children` component. |

## Style

By default, CardWindow has the style `{ width: '100%', height: '100%' }`. These styles can be overridden.

```jsx live
function App() {
  const items = [...Array(1000)].map((_, i) => i);
  const cardRect = { width: 300, height: 200 };
  const style = { height: 400 };
  return (
    <CardWindow data={items} cardRect={cardRect} style={style}>
      {({ index, style }) => (
        <div className="card" style={{ ...style, height: style.height - 16 }}>
          <div style={{ padding: 16 }}>
            <h2>{index}</h2>
          </div>
        </div>
      )}
    </CardWindow>
  );
}
```

## Loading usage

### Play ground

```jsx live
function App() {
  const items = [...Array(100)].map((_, i) => i);
  const cardRect = { width: 300, height: 200 };
  const loading = { type: 'card', Component: LoadingComponent, loadMore: () => alert('more!') }
  return (
    <div style={{ height: 300 }}>
      <CardWindow data={items} cardRect={cardRect} loading={loading} justifyContent="stretch" >
        {Card}
      </CardWindow>
    </div>
  );
}
```
