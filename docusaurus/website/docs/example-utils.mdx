---
sidebar_position: 3
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Example Utils

[Infma](https://infima.dev/) is used for component styling.

## Navbar

<Tabs>
<TabItem value="js" label="JavaScript">

```jsx
const Navbar = ({ children }) => (
  <nav className="navbar">
    <div className="navbar__inner">
      <div className="navbar__items">{children}</div>
    </div>
  </nav>
);
```

</TabItem>
<TabItem value="ts" label="TypeScript">

```tsx
const Navbar: React.FC = ({ children }) => (
  <nav className="navbar">
    <div className="navbar__inner">
      <div className="navbar__items">{children}</div>
    </div>
  </nav>
);
```

</TabItem>
</Tabs>

## useDropdown

<Tabs>
<TabItem value="js" label="JavaScript">

```jsx
const useDropdown = (label, list, init = 0) => {
  const [index, set] = React.useState(init);
  const element = (
    <div className="navbar__item dropdown dropdown--hoverable">
      <p className="navbar__link">{`${label}: ${list[index]}`}</p>
      <ul className="dropdown__menu">
        {list.map((item, i) => (
          <li key={item}>
            <p className="dropdown__link" onClick={() => set(i)}>{item}</p>
          </li>
        ))}
      </ul>
    </div>
  );
  return [value, element];
};
```

</TabItem>
<TabItem value="ts" label="TypeScript">

```tsx
const useDropdown = (label: string, list: string[], init = 0) => {
  const [index, set] = React.useState(init);
  const element = (
    <div className="navbar__item dropdown dropdown--hoverable">
      <p className="navbar__link">{`${label}: ${list[index]}`}</p>
      <ul className="dropdown__menu">
        {list.map((item, i) => (
          <li key={item}>
            <p className="dropdown__link" onClick={() => set(i)}>{item}</p>
          </li>
        ))}
      </ul>
    </div>
  );
  return [value, element];
};
```

</TabItem>
</Tabs>

## Sample Card

The CardProps definition is [here](./api/modules#cardprops).

<Tabs>
<TabItem value="js" label="JavaScript">

```jsx
const Card = ({ data, index, style }) => (
  <div className="card" style={{ ...style }}>
    <div className="card__body">
      <h2>{data[index]}</h2>
    </div>
  </div>
);
```

</TabItem>
<TabItem value="py" label="TypeScript">

```tsx
const Card: React.FC<CardProps> = ({ data, index, style }) => (
  <div className="card" style={{ ...style }}>
    <div className="card__body">
      <h2>{data[index]}</h2>
    </div>
  </div>
);
```

</TabItem>
</Tabs>
