{
  "name": "card-window-root",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "build": "yarn workspace card-window build",
    "ci": "yarn fix && yarn test",
    "coverage": "yarn workspace card-window test --coverage",
    "deploy": "GIT_USER=michiharu yarn workspace website deploy",
    "fix": "yarn workspace card-window fix",
    "start": "yarn workspace website start",
    "test": "yarn workspace card-window test",
    "watch": "yarn workspace card-window watch"
  },
  "devDependencies": {
    "@types/react": "^17.0.37",
    "@types/react-dom": "^17.0.11",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
